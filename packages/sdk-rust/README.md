# OSS AI Agent Tool - Rust SDK

Client library for interacting with the OSS AI Agent Tool Orchestrator.

## Installation

Add to your `Cargo.toml`:

```toml
[dependencies]
oss-ai-agent-sdk = "0.1.0"
```
